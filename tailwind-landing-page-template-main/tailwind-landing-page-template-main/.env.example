# ─── Required — copy this file to .env.local and fill in the values ───────────
# Never commit .env.local to version control.

# Base URL of the GEQO backend API (no trailing slash).
# Local dev:  http://localhost:8000
# Production: https://api.mygeqo.com
NEXT_PUBLIC_API_URL=https://api.mygeqo.com
